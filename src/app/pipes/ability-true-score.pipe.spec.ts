import { AbilityTrueScorePipe } from './ability-true-score.pipe';

describe('AbilityTrueScorePipe', () => {
  it('create an instance', () => {
    const pipe = new AbilityTrueScorePipe();
    expect(pipe).toBeTruthy();
  });
});
